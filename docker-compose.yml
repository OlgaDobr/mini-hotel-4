services:
  web:
    image: nginx:stable-alpine3.21-perl
    ports:
      - "8000:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./.develop/nginx.conf:/etc/nginx/conf.d/default.conf
    networks: 
      - mini hotel 4-network
   
  php-fpm:
    build: .
    container_name: laravel
    env_file:
      - ./.env
    volumes:
      - ./backend:/app
    networks: 
      - mini hotel 4-network
  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: rootpasword
      MYSQL_DATABASE: hotel
      MYSQL_USER: Olga2005
      MYSQL_PASSWORD: 123456
    volumes:
      - mysql_data:/var/lib/mysql
      - ./.develop/mysql_init:/docker-entrypoint-initdb.d
    ports: 
      - "3306:3306"
    networks: 
      - mini hotel 4-network

  adminer:
    image: adminer:4.8.1
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: mysql
      ADMINER_DEFAULT_USER: Olga2005
      ADMINER_DEFAULT_DB: hotel
      ADMINER_DESIGN: dracula
    networks:
      - mini hotel 4-network
    depends_on:
      - db 

networks: 
  mini hotel 4-network:  
    driver: bridge

volumes:
  mysql_data:


  
 
